PROGRAM_PREFIX	=
CC 		= $(PROGRAM_PREFIX)gcc
OBJDUMP		= $(PROGRAM_PREFIX)objdump
OBJCOPY		= $(PROGRAM_PREFIX)objcopy
MACHFLAGS	=
ASFLAGS		= $(MACHFLAGS)
CFLAGS		= $(MACHFLAGS) -Og -g -Wall
LDSCRIPT	=
LDFLAGS		=
LDLIBS		=

.PHONY: all clean

all: rast_host tri2c

clean:
	rm -f rast_host tri2c

rast_host: LDLIBS += -lm
rast_host: qmath.c host_main.c

tri2c: tri2c.c

rast_host tri2c:
	$(CC) -o $(@) $(CPPFLAGS) $(CFLAGS) $(LDFLAGS) $(^) $(LDLIBS)
